import { DivisionStack } from "@/components/landing/DivisionStack";

export function BeamPublicLanding() {
  return (
    <main className="min-h-screen bg-[#080908] text-[var(--beam-text-primary)]">
      <DivisionStack />
    </main>
  );
}
